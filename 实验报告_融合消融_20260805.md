# 多模态决策级融合与消融实验报告

> 项目：老年人心理健康语音筛查系统（挑战杯）
> 实验日期：2026-08-04 ~ 2026-08-05
> 状态：✅ 完成（479 条全量）

---

## 1. 实验目标

回答三个问题：

1. **声学特征单独能区分抑郁吗？**（EATD 语料上）
2. **LoRA 微调的 MLLM 文本判断准确度如何？**（全量评估）
3. **决策级融合是否优于任何单一通道？**（多模态融合的必要性验证）

## 2. 数据与方法

### 2.1 数据集

| 项目 | 内容 |
|------|------|
| 数据集 | EATD-Corpus（ICASSP2022 抑郁检测数据集） |
| 规模 | 162 人 × 3 情绪（neutral/negative/positive）= 486 条音频 |
| 有效样本 | 479 条（含可读转写文本） |
| 标签 | SDS 自评量表，SDS ≥ 50 判为抑郁（正样本 154 / 负样本 325） |
| 特征 | 8 个全局声学统计特征：语速、停顿比、音高变异性、能量变异性、jitter、shimmer、浊音比、频谱质心 |

### 2.2 三通道架构

```mermaid
graph LR
    A[音频] --> B[声学特征提取<br>librosa + parselmouth]
    B --> C[声学分类器<br>RandomForest / SVM / GB]
    C -->|P_acoustic| E[决策级融合<br>w·P_acoustic + (1-w)·P_text]
    A --> D[Whisper 转写]
    D --> F[Qwen2.5-VL LoRA 微调<br>推理式报告版]
    F -->|P_text 结论二值| E
    E --> G[消融对比]
```

- **声学通道**：8 全局特征 → 3 种分类器对比，**按人 GroupKFold 5 折**输出 out-of-fold 概率（同一人的 3 条音频不会跨训练/验证集，防泄漏）
- **文本通道**：Qwen2.5-VL-7B LoRA 微调（推理式报告版，400 条均衡训练数据，FN 加权 3.0），全量 479 条推理，解析"结论：需要关注/正常"为二值概率
- **融合**：P_fused(w) = w·P_acoustic + (1−w)·P_text，w ∈ {0.0, 0.1, …, 1.0} 网格搜索
- **消融**：纯声学（w=1）/ 纯 LoRA 文本（w=0）/ 最优融合（w*）
- **参照**：零样本纯文本 MLLM 评估（63 条子集，评估集不同，仅作方向性参照）

## 3. 结果

### 3.1 声学分类器对比（按人 GroupKFold，out-of-fold）

| 分类器 | AUC | 说明 |
|--------|-----|------|
| RandomForest | 0.476 | 最优（class_weight=balanced） |
| GradientBoosting | 0.474 | — |
| SVM-RBF | 0.415 | 全部预测为负类 |

> **声学单独 AUC < 0.5（差于随机猜测）**，Sens 仅 9.1%。

### 3.2 消融对比（479 条全量）

| 配置 | Acc | Sens | Spec | F1 | AUC |
|------|-----|------|------|-----|-----|
| 纯声学分类器 (w=1) | 63.7% | 9.1% | 89.5% | 13.9% | **0.471** |
| 纯 LoRA 文本 (w=0) | **73.9%** | **70.8%** | 75.4% | **63.6%** | **0.731** |
| 决策级融合 (w*=0.0) | 73.9% | 70.8% | 75.4% | 63.6% | 0.731 |

### 3.3 融合权重网格（节选）

| w（声学权重） | Acc | Sens | Spec | F1 | AUC |
|-----|-----|------|------|-----|-----|
| 0.0 | 73.9% | 70.8% | 75.4% | 63.6% | **0.731** |
| 0.3 | 73.9% | 70.8% | 75.4% | 63.6% | 0.713 |
| 0.5 | 73.9% | 70.8% | 75.4% | 63.6% | 0.713 |
| 0.7 | 69.5% | 39.6% | 83.7% | 45.5% | 0.709 |
| 1.0 | 63.7% | 9.1% | 89.5% | 13.9% | 0.471 |

> 最优 w = 0.0：任何声学权重都会拉低性能 → **声学通道无增量**。

### 3.4 微调价值对比（零样本 vs LoRA 微调）

| 模型 | 评估集 | Acc | Sens | Spec | F1 |
|------|--------|-----|------|------|-----|
| Qwen2.5-VL 零样本 | 63 条子集* | 85.7% | **25.0%** | 89.8% | 18.2% |
| LoRA 微调（推理式报告版） | 479 条全量 | 73.9% | **70.8%** | 75.4% | 63.6% |

> \* 评估集不同，仅作方向性参照。微调后 **Sensitivity 提升 45.8 个百分点**，F1 提升 45.4 个百分点——零样本"高准确率"是类别不平衡的假象（63 条仅 4 个正样本），微调才真正解决了漏检问题。

## 4. 结论

1. **EATD 上声学特征无抑郁区分力**（AUC 0.471 < 0.5）。原因分析：
   - EATD 是任务式朗读/对话语料，录音条件规范，声学差异小
   - 8 个全局统计特征过于粗糙，丢失了韵律的时序信息
   - SDS 自评标签与语音声学表现的关联本身较弱
2. **LoRA 微调 MLLM 是绝对主力**（AUC 0.731，Sens 70.8%），文本语义信息量 >> 声学统计特征
3. **决策级融合未带来增量**（w*=0），多模态融合的必要性在 EATD 上不成立——**但这恰恰证明了消融实验的价值**：它定量指出了"声学模块当前是无效输入"，为后续改进提供了明确方向

## 5. 局限与后续方向

| 局限 | 影响 | 后续方向 |
|------|------|---------|
| EATD 任务式语料声学差异小 | 声学通道无增量 | 换真实临床语料（DAIC-WOZ）或自然对话语料 |
| 8 个全局统计特征过粗 | 丢失时序韵律信息 | 加时序特征（帧级特征均值/方差/斜率）、eGeMAPS 特征集（需 openSMILE） |
| LoRA 训练数据含 EATD 文本改造（合成 400 条） | 测试集存在部分文本泄漏 | 用 DAIC-WOZ / CMDC 独立验证；或人工标注真实语料微调 |
| 零样本参照评估集不一致（63 vs 479） | 无法直接对比 | 补跑零样本全量评估 |
| 二值结论丢失概率信息 | 融合粒度粗 | 训练带概率输出的分类头（如 softmax 温度校准） |

## 6. 系统意义

- 当前系统（pipeline v2）的**有效路径是：音频 → Whisper 转写 → LoRA MLLM 判断**（Sens 70.8%）
- 声学特征暂不能单独支撑筛查结论，但在真实老年语料（说话慢、停顿多、环境嘈杂）中可能重新有区分力——需在真实场景验证
- 本实验提供了完整的**消融证据链**，证明微调的必要性（+45.8pp Sens），回答评审"为什么要微调/多模态有什么用"的质疑

---

**复现**：`scripts/fusion_acoustic.py` → `scripts/eval_lora_full.py`（双分片）→ `scripts/fusion_ablation.py`
**产物**：`training_output/fusion/p_acoustic.json`、`eval_full/*.jsonl`（479 条）、`ablation_result.json`
