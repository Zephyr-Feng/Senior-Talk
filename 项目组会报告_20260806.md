# Senior-Talk 老年人心理健康语音筛查系统
## 项目阶段总结报告（第 3 期）

**日期**：2026-08-06 | **项目状态**：融合消融实验完成，明确模型有效路径与改进方向

---

## 一、项目概述

> 基于语音声学特征与多模态大模型（MLLM）的老年人心理健康筛查系统。
> 将授权范围内的语音转化为可量化的生理与心理学特征，结合 Qwen2.5-VL 多模态大模型进行抑郁风险评估，输出可解释的诊断报告。

**数据集**：EATD-Corpus（ICASSP2022 中文抑郁检测数据集）— 162 人 × 3 种情绪，486 段音频，SDS 标签（SDS≥50 为抑郁）

---

## 二、近期工作进展（自 07-21 组会以来）

| # | 工作项 | 状态 | 结果 |
|---|--------|:----:|------|
| 1 | **融合消融实验（479 条全量）** | ✅ | 声学 AUC 0.471 / LoRA 文本 AUC 0.731 / 融合 w*=0 |
| 2 | **LoRA 全量评估**（从 80 条子集 → 479 条全量） | ✅ | Acc 73.9%, Sens 70.8%, F1 63.6% |
| 3 | **孤独感信号检测模块**（ULS-8 六维度） | ✅ | 规则引擎 + 训练数据增强，12/12 单测 |
| 4 | **孤独规则引擎 EATD 全量验证** | ✅ | 0 强信号 / 17 误报 / 1 漏检 → 阈值保守设计有效 |
| 5 | 本地 Qwen2.5-VL 推理验证（CUDA 12.8） | ✅ | GPU 实测通过 |
| 6 | 50 段人工核对 | 🗑️ | 已取消（无此验收要求） |

---

## 三、融合消融实验（本期核心工作）

### 3.1 目的

回答三个问题：① 声学特征单独能否区分抑郁？② LoRA 微调 MLLM 判断精度？③ 决策级融合是否优于单一通道？

### 3.2 方法

```
音频 → 声学特征(8项) → 分类器(RF/SVM/GB) ──→ P_acoustic ──┐
音频 → Whisper 转写 → Qwen2.5-VL LoRA ──────────→ P_text ──┼→ P_fused = w·P_acoustic + (1-w)·P_text
                                                              └→ w 网格搜索 [0,1]，消融对比
```

- **声学通道**：按人 GroupKFold 5 折 out-of-fold 概率（同一人 3 条音频不跨训练/验证集，防泄漏）
- **文本通道**：LoRA 推理式报告版全量推理 479 条，解析"结论：需要关注/正常"为二值
- **消融**：纯声学（w=1）/ 纯文本（w=0）/ 最优融合（w*）

### 3.3 结果（479 条全量，正 154 / 负 325）

| 配置 | Acc | Sens | Spec | F1 | AUC |
|------|:---:|:---:|:---:|:---:|:---:|
| 纯声学分类器（RandomForest） | 63.7% | 9.1% | 89.5% | 13.9% | **0.471** |
| 纯 LoRA 文本 | **73.9%** | **70.8%** | 75.4% | **63.6%** | **0.731** |
| 决策级融合（w*=0） | 73.9% | 70.8% | 75.4% | 63.6% | 0.731 |
| 参照：零样本 MLLM（63 条子集*） | 85.7% | 25.0% | 89.8% | 18.2% | — |

> *评估集不同，仅作方向性参照。

### 3.4 关键结论

1. **声学特征在 EATD 上无抑郁区分力**（AUC 0.471 < 随机 0.5）：任务式朗读语料声学差异小 + 8 个全局统计特征过粗 + SDS 标签与语音关联弱
2. **LoRA 微调 MLLM 是绝对主力**（AUC 0.731）：语义内容的信息量远超声学统计特征
3. **决策级融合无增量**（最优 w=0）：声学概率不携带有效信号，任何权重都拉低性能
4. **微调价值明确**：零样本 Sens 25% → LoRA 微调 70.8%（**+45.8pp**），微调解决漏检问题

---

## 四、孤独感信号检测模块（新增）

### 4.1 设计

- **ULS-8 六维度关键词映射**：缺乏陪伴 / 无人倾诉 / 被冷落 / 无法融入 / 孤单感受 / 被遗弃
- **否定/适应语境过滤**："不是一个人"、"一个人也挺好"等不计数
- **维度计数强信号判定**：≥2 维度，或 ≥1 维度且含明确孤独词汇（孤独/孤单/寂寞/孤零零）才算"明显孤独倾向"
- **保守原则**：单维度单关键词不触发，避免误报

### 4.2 集成方式

训练数据报告生成器中，孤独信号明显时：
- 分析部分追加"可能存在孤独情绪"（带原文引用）
- 建议部分追加"评估 ULS-8 / 鼓励社交互动"
- **结论行保持抑郁主判断不变** → 下游评估解析零改动

### 4.3 EATD 全量验证（479 条）

| 统计项 | 结果 |
|--------|:----:|
| 强信号 | **0 条**（EATD 为任务式朗读语料，不含孤独内容） |
| 维度命中 | 18 条（全为"缺乏陪伴"维度，"一个人"触发） |
| 误报 | 17/18（"一个人"多义性：一个人认为/一个人走走挺开心/一个人学习） |
| 真信号被阈值拦截 | 1 条（t_72"妈妈来不了，一个人过年不开心"——单维度，未达强阈值） |

**结论**：强信号阈值成功阻止了 17 条误报升级为"提示"——保守设计生效。但规则引擎的召回在真实自然对话中会不足（含蓄表达无法命中），孤独维度的可靠检测需 MLLM 语义分析（待训练数据带孤独标签后验证）。

---

## 五、基线对比总表（累计）

| 方案 | Acc | Sens | Spec | F1 | 备注 |
|------|:---:|:---:|:---:|:---:|------|
| 纯声学特征 sklearn | — | — | — | — | AUC 0.605 → 0.471（全量 479 条严格评估） |
| Qwen2.5-VL 零样本（文本） | 85.7% | 25.0% | 89.8% | 18.2% | 高 Acc 是类别不平衡假象，漏检严重 |
| LoRA 微调（推理式报告）80 条 | 80.0% | 71.4% | 84.6% | 71.4% | 子集评估（偏乐观） |
| **LoRA 微调（推理式报告）479 条全量** | **73.9%** | **70.8%** | 75.4% | **63.6%** | **现行有效方案** |

---

## 六、局限与待解决问题

| 局限 | 影响 | 解决方案 |
|------|------|---------|
| EATD 非老年语料（年轻被试） | 声学差异小，声学通道无增量；与目标场景错位 | ① 真实采集中文老年语音 + GDS-15 量表（已规划）② DAIC-WOZ 英文基准验证（申请中） |
| 8 个全局声学统计特征过粗 | 丢失韵律时序信息 | 帧级时序特征 / eGeMAPS（openSMILE） |
| LoRA 训练数据含 EATD 文本改造 | 测试存在部分文本泄漏，全量 73.9% 可能偏高 | 真实语料独立验证 |
| 孤独检测为规则引擎 | 含蓄表达漏检 | 训练数据加孤独标签 → LoRA 语义判断 |

---

## 七、下一步计划

**短期（本周）**
- [ ] 真实采集方案设计：中老年志愿者 30-50 人，5 分钟自由话题录音 + GDS-15/PHQ-9/ULS-8 施测
- [ ] DAIC-WOZ 申请提交（1-2 周批复，英文基准对照）

**中期（1-2 周）**
- [ ] 声学特征升级：时序特征 + eGeMAPS，在 EATD 上重验声学通道
- [ ] 训练数据加入孤独标签，LoRA 支持孤独语义判断
- [ ] 真实数据采集执行 + pipeline 实测

**长期**
- [ ] 中文老年抑郁语音基线数据建成（比赛差异化亮点）
- [ ] 报告与量表一致性验证、GitHub 开源

---

## 八、待组会讨论问题

1. **真实采集的可行性**：志愿者渠道（社区/敬老院/亲属）、伦理审批、录音设备要求——是否有校内资源可支持？
2. **声学通道是否值得继续投入**：EATD 上声学无效，但换真实老年语料后可能有效。投入 eGeMAPS/时序特征 vs 专注 MLLM 语义路线，如何取舍？
3. **比赛数据叙事**：用"英文基准（DAIC-WOZ）+ 自建中文老年数据"双线结构是否合适？

---

*报告生成时间：2026-08-06 | 数据来源：AutoDL 服务器 + 本地代码库 | 完整实验细节见《实验报告_融合消融_20260805.md》*
